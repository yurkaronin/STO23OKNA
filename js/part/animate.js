/* gsap.registerPlugin(ScrollTrigger, ScrollToPlugin);


var tl = gsap.timeline({
  defaults: {
    duration: 1,
    ease: 'back',
    y: 30,
  }
});

tl.from('.js-header', {
    scale: 0.3,
    y: -50,
    opacity: 0,
  })
  .from('.hero__title', {
    opacity: 0,
  })
  .from('.benefits__item-flex', {
    opacity: 0,
    stagger: 0.2,
  }); */